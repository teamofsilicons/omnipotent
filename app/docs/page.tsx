import { Docs } from "./docs"

export const metadata = {
  title: "documentation — silicon omni",
  description: "Every command omni exposes, what it takes, and what it does underneath.",
}

export default function Page() {
  return <Docs />
}
